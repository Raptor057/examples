# Copia este archivo a .env si quieres cambiar los valores por defecto de docker-compose.
# .env NO se versiona (ver .gitignore de la raiz del repo); este ejemplo si.
POSTGRES_DB=saas_access_audit
POSTGRES_USER=saas
POSTGRES_PASSWORD=saas_dev_password
POSTGRES_PORT=55440
