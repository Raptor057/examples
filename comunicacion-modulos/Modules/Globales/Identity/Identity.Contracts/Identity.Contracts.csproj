<Project Sdk="Microsoft.NET.Sdk">

  <!-- Contrato del global Identity. Es
       el accessor del usuario actual, que TODOS los modulos consumen. Sin deps. -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
